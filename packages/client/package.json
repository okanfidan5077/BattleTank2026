{
  "name": "@battletank/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phaser browser client for BattleTank2026.",
  "scripts": {
    "build": "tsc -b && vite build",
    "clean": "tsc -b --clean",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@battletank/shared": "*",
    "@colyseus/schema": "^3.0.76",
    "colyseus.js": "^0.16.22",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
